<div class="window">
  <div id="description">
    <h1><%= van.make.upcase %> <%= van.model.upcase %></h1>
    <h2>Listed by <strong><%= User.find(van.user_id).first_name %></strong></h2>
    <h2>Price: £<%= number_with_delimiter(van.price) %></h2>
    <br>
    <p><%= van.description %></p>
  </div>

  <div id="photo">
    <%= cl_image_tag van.photo, width: 100, height: 150, crop: :fill %>
  </div>
</div>
